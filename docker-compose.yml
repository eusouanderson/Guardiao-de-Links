services:
  linksaved:
    build: .
    ports:
      - '8000:8000'
    volumes:
      - ./src/data:/app/src/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
